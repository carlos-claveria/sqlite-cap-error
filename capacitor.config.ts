import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.siliconmedia.silmob',
  appName: 'silmob',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
